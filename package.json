{
  "name": "scan2bim-converter",
  "version": "0.1.0",
  "description": "Scan2BIM desktop converter: E57 to Potree (Tauri 2)",
  "author": "Sohail Saifi",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build --config src-tauri/tauri.bundle.conf.json",
    "package:windows": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/package-windows.ps1",
    "package:linux":   "bash scripts/package-linux.sh",
    "package:macos":   "bash scripts/package-macos.sh",
    "build:windows": "npm run build && npm run package:windows",
    "build:linux":   "npm run build && npm run package:linux",
    "build:macos":   "npm run build && npm run package:macos"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.1.0"
  },
  "dependencies": {
    "@tauri-apps/api": "2.1.1",
    "@tauri-apps/plugin-dialog": "2.0.1",
    "@tauri-apps/plugin-shell": "2.0.1",
    "@tauri-apps/plugin-fs": "2.0.3"
  }
}
